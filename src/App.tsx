import React from 'react';

import { View } from 'react-native';

import Theme from './theme';

const src: React.FC = () => {
  return (
    <Theme>
      <View />
    </Theme>
  );
};

export default src;
